# Custom Headlines

An administrator can queue an exact headline. The provider prompt must preserve it while generating the tagline, summary, metadata, and article body.

Exact duplicates and headlines containing unsupported guarantees are held before provider generation. Custom-headline work is classified as manual and does not consume automatic quotas.
