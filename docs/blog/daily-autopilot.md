# Daily Autopilot

Autopilot is disabled by default. When enabled on the worker, one discovery job is created per six-hour bucket. A run selects zero, one, or two worthwhile opportunities, with a default maximum of two automatic articles per day and a configurable weekly limit.

No article is generated merely to fill a quota. Administrator-triggered work never consumes the automatic quota.
