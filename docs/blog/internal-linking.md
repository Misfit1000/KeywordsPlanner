# Internal Linking

Every full article requires at least two descriptive, crawlable internal links. Links should connect the reader to a relevant audit workflow, product page, topic hub, or published article.

Publication checks reject malformed internal paths. Related blog destinations are selected only from published articles and rendered with canonical `/blog/{slug}` paths.
