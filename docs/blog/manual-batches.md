# Manual Batches

A manual batch accepts one to five distinct headlines. Each headline creates an independent leased job, so one failure does not partially publish or stop the other jobs.

Repeated headlines in one batch and recent duplicate batches are rejected. Batch articles have their own count and do not consume automatic quotas.
