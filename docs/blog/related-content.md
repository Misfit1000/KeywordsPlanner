# Related Content

Related articles are stored as post ID, canonical slug, title, and a short relevance reason. Public article HTML renders each item as a standard hyperlink.

When no reviewed relationship is stored, the server may select recent published articles from the same topic cluster. Draft, failed, archived, and future scheduled posts are excluded.
